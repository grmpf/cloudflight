/**
 * Adjust quality and power consumtion to improve performance (based on environment)
 * @param gl
 * @param performance
 */
const performanceAdjustments = (gl, performance) => {
	if(performance) {
		//performance...
	}
}

export {performanceAdjustments}
