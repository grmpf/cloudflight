/**
 * Disabled raycaster - can be used when nothing inside the <canvas> needs to be interacted with (e.g. hover on mesh).
 *
 * NOTE: if this is really mesh-based it would be possible to traverse through the scene and set the raycaster on each mesh
 *
 * @param raycaster
 */
const raycasterDisabled = (raycaster) => {
	if(raycaster) {
		//raycaster...
	}
}

export {raycasterDisabled}
