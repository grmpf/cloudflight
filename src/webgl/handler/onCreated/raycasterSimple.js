/**
 * Faster, slightly less accurate raycaster - when performance is more important than accuracy
 *
 * NOTE: if this is really mesh-based it would be possible to traverse through the scene and set the raycaster on each mesh
 *
 * @param raycaster
 */
const raycasterSimple = (raycaster) => {
	if(raycaster) {
		//raycaster...
	}
}

export {raycasterSimple}
