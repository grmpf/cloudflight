/**
 * Less accurate raycaster using only boundingBoxes
 *
 * NOTE: if this is really mesh-based it would be possible to traverse through the scene and set the raycaster on each mesh
 *
 * @param raycaster
 */
const raycasterBoundingBox = (raycaster) => {
	if(raycaster) {
		//raycaster...
	}
}

export {raycasterBoundingBox}
